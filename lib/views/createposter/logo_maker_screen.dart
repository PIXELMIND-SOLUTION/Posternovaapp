import 'package:flutter/material.dart';

class LogoMakerScreen extends StatelessWidget {
  const LogoMakerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}